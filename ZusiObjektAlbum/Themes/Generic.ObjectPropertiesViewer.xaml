<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ZusiObjektAlbum.Controls">

    <local:AuthorsHeaderConverter x:Key="AuthorsHeaderConverter"/>

    <Style TargetType="{x:Type local:ObjectPropertiesViewer}">
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ObjectPropertiesViewer}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" Style="{StaticResource PropertyTitleStyle}">
                                <Run Text="Objekt "/><Run Text="{Binding Name, Mode=OneWay}" FontWeight="Normal"/>
                            </TextBlock>
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <Grid Grid.IsSharedSizeScope="True">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Expander Header="{Binding Authors, Converter={StaticResource AuthorsHeaderConverter}}" Style="{StaticResource PropertyExpanderStyle}">
                                        <Grid>
                                            <ListBox ItemsSource="{Binding Authors, Mode=OneWay}" BorderThickness="0" Background="{DynamicResource ExpandSite.Background}" HorizontalContentAlignment="Stretch"
                                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled" Padding="-2">
                                                <ListBox.ItemContainerStyle>
                                                    <Style TargetType="{x:Type ListBoxItem}">
                                                        <Setter Property="Margin" Value="0"/>
                                                        <Setter Property="Padding" Value="0"/>
                                                    </Style>
                                                </ListBox.ItemContainerStyle>
                                                <ListBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="*"/>
                                                                <RowDefinition Height="Auto"/>
                                                            </Grid.RowDefinitions>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition SharedSizeGroup="A"/>
                                                                <ColumnDefinition SharedSizeGroup="B1"/>
                                                                <ColumnDefinition SharedSizeGroup="B2"/>
                                                                <ColumnDefinition Width="*"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Text="Name" Style="{StaticResource PropertyNameStyle}"/>
                                                            <TextBlock Grid.Column="1" Grid.ColumnSpan="3" Style="{StaticResource PropertyTextStyle}" Text="{Binding Name}"/>
                                                            <TextBlock Grid.Row="1" Text="E-Mail" Style="{StaticResource PropertyNameStyle}" 
                                                                       Visibility="{Binding EMail, Converter={StaticResource EmptyStringToVisibilityConverter}}"/>
                                                            <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Style="{StaticResource PropertyTextStyle}" Text="{Binding EMail}"
                                                                       Visibility="{Binding EMail, Converter={StaticResource EmptyStringToVisibilityConverter}}"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListBox.ItemTemplate>
                                            </ListBox>
                                        </Grid>
                                    </Expander>
                                    <Expander Header="Abmessungen" Grid.Row="1" Style="{StaticResource PropertyExpanderStyle}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition SharedSizeGroup="A"/>
                                                <ColumnDefinition SharedSizeGroup="B1"/>
                                                <ColumnDefinition SharedSizeGroup="B2"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="Länge" Style="{StaticResource PropertyNameStyle}"/>
                                            <TextBlock Grid.Column="1" Grid.ColumnSpan="3" Style="{StaticResource PropertyTextStyle}" Text="{Binding Length, Mode=OneWay, StringFormat={}{0:F2}m}"/>
                                            <TextBlock Text="Breite" Grid.Row="1" Style="{StaticResource PropertyNameStyle}"/>
                                            <TextBlock Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="3" Style="{StaticResource PropertyTextStyle}" Text="{Binding Width, Mode=OneWay, StringFormat={}{0:F2}m}"/>
                                            <TextBlock Text="Höhe" Grid.Row="2" Style="{StaticResource PropertyNameStyle}"/>
                                            <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" Style="{StaticResource PropertyTextStyle}" Text="{Binding Height, Mode=OneWay, StringFormat={}{0:F2}m}"/>
                                        </Grid>
                                    </Expander>
                                    <Expander Header="Geometrie" Grid.Row="2" Style="{StaticResource PropertyExpanderStyle}">
                                        <local:GeometryViewer/>
                                    </Expander>
                                </Grid>
                            </ScrollViewer>
                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>